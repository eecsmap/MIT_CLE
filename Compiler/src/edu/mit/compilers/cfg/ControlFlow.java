package edu.mit.compilers.cfg;

import edu.mit.compilers.defs.Defs;

// if, else, while, for
public class ControlFlow {
    Defs.Type type;

    public ControlFlow(Defs.Type type) {
        this.type = type;
    }
}
